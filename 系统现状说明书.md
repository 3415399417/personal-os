# BetterLife AI Personal OS — 系统现状说明书

> 写给系统的主人（哥哥）：用 AI 架构师的视角，看懂你自己的系统。
> 版本对应：2026-08-27（桌面端 0.2.0 / Next.js 15 + React 19 + Prisma 7 + SQLite / Tauri 2 桌面壳）
> 配套文档：`ARCHITECTURE.md`（开发规范）、`PROJECT-ROADMAP.md`（历史路线图）

---

## 0. 一句话定位

**这是一个跑在你电脑上的"个人操作系统"**：任务、项目、笔记、复盘、学习、资产、提醒、情报全部收进一个本地系统，AI 负责自动干杂活（写日报、盯情报、对话操作数据），桌面窗口就是你的"基地"。

核心原则：**本地优先（Local First）**——数据全在你电脑里，不上云，不联网也能用（AI 功能除外）。

---

## 1. 系统地图：五层架构

```
┌─────────────────────────────────────────────────────────┐
│ ① 展示层（浏览器 / 桌面 WebView 窗口）                     │
│    app/ 页面 ×16 · components/ 组件 · app/styles/ 样式     │
├─────────────────────────────────────────────────────────┤
│ ② 前端逻辑层（hooks）                                     │
│    自动备份 / 用量计时 / GitHub检测 / 快捷键 / 桌面通知…    │
├─────────────────────────────────────────────────────────┤
│ ③ 接口层（app/api/）                                     │
│    /api/data（统一数据入口 + 参数校验）                   │
│    /api/chat（AI 对话流式）· /api/github · /api/report…  │
├─────────────────────────────────────────────────────────┤
│ ④ 业务逻辑层（lib/db-actions/ 按业务域拆 8 个模块）       │
│    所有"增删改查 + 规则计算"都在这层                      │
├─────────────────────────────────────────────────────────┤
│ ⑤ 数据层：SQLite 数据库（dev.db）+ backup/ 自动备份        │
└─────────────────────────────────────────────────────────┘
         ↕ 外部服务（按需）：
         DeepSeek API（AI 对话/日报/孵化）
         GitHub API（情报）· Hacker News（新闻）
         QQ/Gmail IMAP（邮箱，规划中）
```

**一条数据是怎么流动的**（以"勾选任务"为例）：
你点勾 → 前端 `lib/api.ts` 封装函数 → POST `/api/data`（带 action= toggleTask）→ 参数校验 → `db-actions/tasks.ts` 改数据库 → 返回 `{ok, data}` → 页面刷新 + 广播 `betterlife:data-changed` 事件（侧边栏等全部同步更新）。

---

## 2. 数据库：16 张表，各管一摊

| 表 | 存什么 | 备注 |
|---|---|---|
| Project | 项目（名称/状态/进度/文件夹路径/今日焦点） | 进度 = 完成任务÷总任务，自动算 |
| Task | 任务（状态/分组/项目归属/预期产物/今日焦点） | group 四组：必须完成/进行中/等待/已完成 |
| ProgressEvent | 进度事件流水（文件命中/状态变化/确认完成） | 进度感知的时间线 |
| Note | 笔记（类型/内容/关联项目） | note/document/prompt/learning/idea |
| Resource | 收集箱 + 资源库（inbox/领域库/项目库/学习库…） | type 区分 |
| Asset | 长期资产（SOP/Prompt/Skill/项目记忆/复盘记录） | 上色分类 |
| Review | 复盘记录（含 AI 日报/周报） | autoKey 唯一键防重复 |
| Reminder | 提醒（时间/状态） | 服务端每分钟检查 |
| LearningRecord | 学习记录（标题/进度） | |
| FrictionLog | 摩擦日志（卡点/踩坑） | 喂给日报做分析 |
| Notification | 通知中心（系统事件汇总） | 已读字段已弃用 |
| GithubCheck | GitHub 检测快照（已见过的 id 列表） | 防重复通知 |
| DailyUsage | 每日使用时长 | 打开即计时 |
| Profile | 个人资料（姓名/头像/焦点） | 单行，id=main |
| AiConversation / AiMessage | AI 会话 + 消息 | 刷新不丢 |

---

## 3. 前端地图：16 个页面

| 路由 | 页面 | 干什么 |
|---|---|---|
| `/` | 首页 | 六大卡片（项目/笔记/执行/学习/资产/生活）+ Hero 焦点 + 快速入口 |
| `/today` | 今天 | 今日总进度 + 四个任务分组 + 今日焦点项目 |
| `/projects` | 项目 | 项目列表 + 历史项目扫描导入 + 文档孵化 |
| `/projects/[id]` | 项目详情 | 任务 + 进度感知（产物就位→确认完成）+ 笔记 + 时间线 + 摩擦日志 |
| `/review` | 复盘 | 复盘列表 + AI 日报/周报一键生成 |
| `/notes` | 笔记 | 全部笔记 + 项目关联 |
| `/inbox` | 收集箱 | 随手扔想法，一键处理/入库 |
| `/learning` | 学习中心 | 学习记录 + 时长趋势 |
| `/assets` | 资产库 | SOP/Prompt/Skill 等长期资产 |
| `/resources/[type]` | 资源子页 | 领域库/知识库/指令库/模板库 |
| `/stats` | 统计看板 | 任务/项目/学习统计 |
| `/github` | GitHub 情报 | 热门仓库/插件/模型/Agent/Harness + 技术新闻 |
| `/ai` | AI 对话 | DeepSeek 聊天（Flash/Pro）+ 工具操作数据库 |
| `/space` | 个人空间 | 名片/数据概览/最近动态 + 徽章入口 |
| `/space/badges` | 徽章墙 | 26 个成就徽章 |
| `/settings` | 设置 | 自动保存 + 快捷键录制 + 通知开关 + 备份/还原 |
| `/workbench` | 工作台 | 快捷入口聚合 |

**全局骨架**：`AppShell`（外壳）→ `Sidebar`（待办/今日状态/提醒 + 个人空间入口）+ `Header`（导航/搜索 ⌘K/通知铃铛/头像）。所有页面共用。

---

## 4. 后台自动化：系统"自己偷偷干的活"

**A. 服务端调度器（`instrumentation.ts`，进程常驻，浏览器关着也跑）**
| 任务 | 时间 | 逻辑 |
|---|---|---|
| AI 日报 | 每天 20:00-20:05 | 幂等生成（数据库唯一键兜底，每天最多一条） |
| AI 周报 | 每周日 20:00 | 同上 |
| 到期提醒 → 通知 | 每分钟 | 检查 Reminder 表 |
| 启动补跑 | 服务启动时 | 20:00 后启动则立即补生成日报/周报 |

**B. 前端钩子（浏览器/桌面窗口打开时跑）**
| 钩子 | 干什么 |
|---|---|
| useAutoBackup | 每天自动备份数据库到 backup/（服务端去重，一天一次） |
| useUsageTimer | 打开系统即计时，累计每日使用时长 |
| useGithubAutoCheck | 打开时 + 每 6 小时检测 GitHub 情报（热门/最新更新/新闻），有新增写通知 |
| useShortcuts | 全局快捷键（n 新建任务 / g t 跳今天 / g p 跳项目 / g s 跳设置 / g , 快捷键设置 / g u 用量 / g / 搜索） |
| useDesktopNotifications | 站内通知 → 系统弹窗（设置页可关） |
| useReminderNotifications | 提醒到点 → 通知 |
| useTauriEvent | 桌面壳事件桥接（如 Ctrl+Shift+I 唤起快速捕捉框） |
| useCached | 页面缓存秒开（切回不闪烁，静默刷新） |

**C. 桌面端壳（Tauri，`E:\我的项目\tauri-rust\betterlife-desktop`）**
启动流程：壳 → 拉起 Next.js 生产服务（`npm run start:prod`）→ 等端口就绪 → WebView 加载 `127.0.0.1:3000`。
壳负责：托盘常驻（关闭窗口最小化到托盘）、开机自启、全局快捷键 Ctrl+Shift+I（唤起窗口 + 打开捕捉框）、外部链接用系统浏览器打开、退出时清理服务进程。

---

## 5. 关键设计决策（为什么这样做）

| 决策 | 原因 |
|---|---|
| SQLite 本地数据库 | 个人单机使用，零部署零成本，数据完全私有 |
| 数据不存浏览器 localStorage | 换浏览器/清缓存不丢；localStorage 只放主题/快捷键等偏好 |
| `/api/data` 统一入口 + 参数白名单校验 | 所有数据操作一个入口好维护，校验防手滑写坏数据 |
| 机器检测 + 人工确认（进度感知） | 文件在 ≠ 干完了，机器当侦察兵、人当裁判 |
| 日报幂等（数据库唯一键） | 多窗口/多进程/重启都不会重复生成 |
| GitHub 401 自动降级匿名 | token 失效不阻塞功能（限流降级而已） |
| 前端钩子 + 服务端调度器分工 | 钩子管"你打开系统时的体验"，调度器管"你关着系统也在跑" |
| 备份进 backup/ + git 双保险 | 防手滑，防硬盘意外 |

---

## 6. 数据安全

- 数据库 `dev.db` 在项目根目录；每天自动备份到 `backup/dev-YYYYMMDDHHmmss.db`（保留 14 天自动清理）
- 设置页支持手动备份 + 从备份还原（还原前自动留 .pre-restore 副本）
- 敏感信息（DeepSeek key / GitHub token）只在 `.env.local`，不落库、不上公网
- 系统只监听 127.0.0.1，不对公网开放；桌面端无鉴权但仅本机可达
- ⚠️ 换电脑 = 把 dev.db（+ backup/）拷过去；没有云同步（第一版明确不做）

---

## 7. 已知短板与技术债（2026-08-27 盘点）

**功能层**
- 邮箱接收（QQ/Gmail）——已提需求，待确认细节后开发
- 移动端/PWA 适配——骨架有，手机体验未打磨
- AI 深化——RAG 第二大脑 v2 已完成（8 类语料 + 中文分词 + 打分排序 + 来源标注 + 命中数展示）；语义检索（embedding）待接入阿里云百炼 qwen key
- 专注计时器、任务完成自动进复盘——未做

**代码层**
- `app/projects/[id]/page.tsx` 1285 行、`/api/chat` 849 行——偏大，功能稳定，可拆不急
- `hooks/useGreeting.ts`——早间卡片删除后已无引用（死代码，待删）
- `GITHUB_TOKEN` 已失效（有匿名兜底，重新生成可恢复高限流）
- 桌面壳流程坑：**改代码 → build 后必须重启桌面 App**（旧服务占端口会一直服务旧构建；今天踩了 3 次）

**流程层**
- git 提交习惯：建议"当日事当日提交"（今天 30+ 文件改动还没提交）

---

## 8. 架构师入门地图（怎么读这套代码）

想从"用户"变成"架构师"，按这个顺序读：

1. **先读这张说明书**（现在你在做的事）——知道有什么零件
2. **读 `ARCHITECTURE.md`**——知道规矩（API 两条路怎么选、db-actions 拆分规则、前端规范）
3. **跟着一条数据流走一遍**：`app/today/page.tsx`（界面）→ `lib/api.ts`（封装）→ `app/api/data/route.ts`（入口+校验）→ `lib/db-actions/tasks.ts`（逻辑）→ `dev.db`（数据）
4. **看自动化**：`instrumentation.ts`（服务端调度器）+ `hooks/`（前端钩子）
5. **看外壳**：`tauri-rust/betterlife-desktop/src-tauri/src/lib.rs`（桌面壳怎么拉起一切）

**读代码的三个提问模板**（架构师思维）：
- 这层是干嘛的？它该不该知道下面那层的事？（分层职责）
- 这个功能是"机器自动"还是"人工确认"？边界在哪？（自动化边界）
- 数据从哪来、存到哪、谁有权限改？（数据流）

---

*本说明书随系统演进更新；下次大版本后建议重新生成。*
